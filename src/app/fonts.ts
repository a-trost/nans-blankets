import { Permanent_Marker, Pacifico } from "next/font/google";

export const marker = Permanent_Marker({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});

export const pacifico = Pacifico({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});
